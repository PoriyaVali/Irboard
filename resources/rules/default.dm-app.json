{
  "_readme": [
    "Settings the Doctor Mobile app adopts. Copy this to custom.dm-app.json and",
    "edit that instead; the custom file wins and survives updates, exactly like",
    "default.clash.yaml / default.sing-box.json next to it.",
    "",
    "These are DEFAULTS, not commands. A setting listed here applies only to",
    "users who have never changed it themselves; anyone who has set it keeps",
    "their own value. When a user taps 'reset to defaults' they land back on",
    "whatever this file says at that moment, not on what was compiled into the",
    "app - so this is also how you change the app's behaviour after release.",
    "",
    "Keys are the app's own preference keys, so no translation table has to be",
    "kept in step: mh_* are mihomo settings, mdns_* the DNS-tunnel core,",
    "byedpi_* the anti-DPI helper, and the dashed names sing-box core options.",
    "Unknown keys are ignored, so an older app build simply skips anything it",
    "does not have yet, and lines starting with _ never reach a device.",
    "",
    "Write values in the form the app stores: log-level as a name ('warn'),",
    "url-test-interval in seconds, mdns_compression as its number. A value of",
    "the wrong type is skipped rather than applied.",
    "",
    "Raise 'version' whenever you edit, so it is obvious in logs which document",
    "a device is running.",
    "",
    "Every value below was checked against the app's own default, so publishing",
    "this file as-is changes nothing until you actually edit something.",
    "",
    "'force' is the exception to 'the user keeps the last word'. Use it when a",
    "setting has stopped working in the field and has to be corrected even on",
    "phones whose owners already changed it:",
    "",
    "  false                          off - the normal, and the default",
    "  [\"byedpi_args\", \"mh_stack\"]     put these back on every profile update",
    "  true                           put ALL of the settings below back",
    "",
    "A forced setting is put back each time the user's profile updates - it is",
    "not locked, so between updates they can still change it and see the change",
    "take effect. Prefer the list form: 'true' overwrites every setting anyone",
    "has ever chosen, including their core and their DNS, on every update. Keys",
    "listed but not defined below are ignored, so a typo cannot wipe a setting",
    "back to something nobody chose."
  ],

  "version": 1,

  "force": false,

  "settings": {
    "_1_core": "which core the connect button drives: mihomo | singbox | mdns",
    "selected_core": "mihomo",

    "_2_singbox": "sing-box core",
    "log-level": "warn",
    "remote-dns-address": "tcp://8.8.8.8",
    "independent-dns-cache": true,
    "enable-fake-dns": false,
    "allow-connection-from-lan": false,
    "connection-test-url": "http://captive.apple.com/hotspot-detect.html",
    "url-test-interval": 600,
    "mtu": 9000,
    "strict-route": true,
    "tun-implementation": "gvisor",

    "_3_mihomo": "mihomo core",
    "mh_ipv6": false,
    "mh_iran_bypass": true,
    "mh_block_ads": true,
    "mh_dns_mode": "fakeip",
    "mh_dns_server": "8.8.8.8",
    "mh_dns_server2": "1.1.1.1",
    "mh_log_level": "warning",
    "mh_stack": "gvisor",
    "mh_mtu": 9000,
    "mh_auto_interval": 120,
    "mh_auto_tolerance": 50,
    "mh_unified_delay": true,
    "mh_tcp_concurrent": true,
    "mh_fakeip_filter": true,
    "mh_sniffer": true,
    "mh_dns_fallback": true,

    "_4_mdns": "MasterDnsVPN core",
    "mdns_resolver_mode": "recursive",
    "mdns_packet_dup": 4,
    "mdns_racing": 3,
    "mdns_balancing": 3,
    "mdns_log_level": "INFO",
    "mdns_compression": 1,

    "_5_antidpi": "anti-DPI helper; --dm lets it pick a method per network",
    "byedpi_enabled": false,
    "byedpi_args": "--dm"
  }
}
